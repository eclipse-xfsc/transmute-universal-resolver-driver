replicaCount: 1
name: transmute-universal-resolver-driver

image:
  repository: node-654e3bca7fbeeed18f81d7c7.ps-xaas.io/dev-ops
  name: transmute-universal-resolver-driver
  # -- Image tag
  # Uses .Chart.AppVersion if empty
  tag: "latest"
  # -- Image sha, usually generated by the CI
  # Uses image.tag if empty
  #sha: ""
  # -- Image pull policy
  pullPolicy: Always
  # -- Image pull secret when internal image is used
  #pullSecrets: key

podAnnotations: {}

service:
  port: 3000
  type: ClusterIP
